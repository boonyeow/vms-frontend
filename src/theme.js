import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    action: {
      lighter: "#626262",
      light: "#555555",
      dark: "#000000",
      main: "#262626",
      contrastText: "#fff",
    },
  },
});

export default theme;
